const myNoun = "dog";
const myAdjective = "big";
const myVerb = "ran";
const myAdverb = "quickly";

// Cambia solo el código debajo de esta línea

const wordBlanks = "The" + " big " + "dog" + " quickly" + " ran";
// Cambia solo el código encima de esta línea
console.log(wordBlanks);
